export namespace Role {
	{{#each roles}}
	{{#if this.description}}// {{this.description}}{{/if}}
	export const {{this.key}} = "{{this.value}}";
	
	{{/each}}
}

export type Role = {{#each roles ~}}
	{{#if @index}} | {{/if}}typeof Role.{{this.key}}
{{~/each}};
